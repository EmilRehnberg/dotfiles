#!/bin/bash
shopt -s expand_aliases
source ~/.bash_profile
source "$(relative_path)/usages/geeknote.sh"

if [ $# -gt 0 ]; then
  geeknote create --title $* --content '#'
  geeknote edit --note "$1"
else
  usage
fi

