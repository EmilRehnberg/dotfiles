" remap leader (default: \)
let mapleader=','
let maplocalleader=';'
noremap <Leader>c :nohlsearch<CR>

" neovim changes
"
" * instead of Y use yy (the latter works everywhere)
" use <TAB> for completion menues (instead of C-n/p)
